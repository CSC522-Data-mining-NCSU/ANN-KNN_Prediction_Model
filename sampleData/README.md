# ProcessSampleData.R
Preprocess the raw data from test dataset(100,000 records). Split the data into two parts for building KNN model. The whole processed data are used for building ANN model.

# ProcessTrainTestSampleData.R
Preprocess the raw data from training dataset(70,000 records) and test dataset(30,000 records).
